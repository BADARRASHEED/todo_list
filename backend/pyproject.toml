[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for a full-stack todo list app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.1",
]

[tool.fastapi]
entrypoint = "main:app"
