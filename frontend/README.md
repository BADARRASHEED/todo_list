# Todo Desk Frontend

Next.js App Router frontend using Tailwind and shadcn-style UI components.

## Run locally

```powershell
npm.cmd install
Copy-Item .env.example .env.local
npm.cmd run dev
```

The app reads the backend URL from `NEXT_PUBLIC_API_URL`.
